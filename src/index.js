// index.js
import count from './count.js';

console.log(count());
console.log(count()); 
console.log(count());
console.log(count(5));

// I wanted to use a class for count but it would've required to change this call syntax